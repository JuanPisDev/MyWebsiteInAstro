---



---
<nav id="navSection">

        <button class="visionMode" id="themeToggle">JP</button>
        <!-- <div id="overlay" class="overlay"></div> -->
        <!-- <div class="sideBar" id="sideBar">
            <button class="visionMode" id="themeToggle" >
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path class="sun" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
                    <path class="moon" d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"></path>
                </svg>

            </button>
            <ul>
                <li>Idioma</li>
                <li>Blog</li>
            </ul>
            <button class="cerrarSideBar" id="closeBtn">✖</button>
        </div> -->
        <ul>
            <li><a href="/sobreMi.html">Sobre Mí</a></li>
            <li><a href="/projects">Proyectos</a></li>
            <li><a href="/#skillsSection">Habilidades</a></li>
        </ul>

</nav>

<script is:inline>
    const openBtn = document.getElementById('openBtn');
    const closeBtn = document.getElementById('closeBtn');
    const sideBar = document.getElementById('sideBar');
    const overlay= document.getElementById('overlay')

    openBtn?.addEventListener('click', () => {
        sideBar?.classList.toggle('activo');
        overlay?.classList.add('activo');
    });

    function openMenu(){
        sideBar?.classList.add('activo');
        overlay?.classList.add('activo');
    }

    function closeMenu(){
        sideBar?.classList.remove('activo');
        overlay?.classList.remove('activo');
    }

    openBtn?.addEventListener('click', openMenu);
    closeBtn?.addEventListener('click', closeMenu);
    
    document.getElementById('themeToggle')?.addEventListener('click', handleToggleClick);


</script>

<style>

    *{
    margin:0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
    }
    nav {
        height: 10vh;
        align-items: center;
        display: flex;
        justify-content: space-evenly;
        margin: 2% 0 2% 0;
        position: sticky;
        top: 2%;
        z-index: 1000;
    }

    nav button{
        width: 10vh;
        height: 10vh;
        background-color: rgb(119, 221, 119);
        border-radius: 50%;
        box-shadow: 0 4px 6px rgba(0,0,0,0.2), 0 10px 20px rgba(0,0,0,0.2);
        border: none;
        font-size: 1.5rem;
        font-weight: bold;
        cursor: pointer;
        color: whitesmoke;
        text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5)
    }
    nav button:hover{
        background-color: rgb(65, 138, 65);
        box-shadow: 0 4px 10px rgba(0,0,0,0.7), 0 10px 20px rgba(0,0,0,0.2);

    }

    :global(html.dark) nav button{
        background-color: rgb(65, 138, 65);
    }

    :global(html.dark) nav button:hover{
        background-color: rgb(119, 221, 119);
    }

    nav ul{
        height: 100%;
        width: 70%;
        list-style-type: none;
        display: flex;
        flex-wrap: wrap;
        align-items: ;
        justify-content: space-around;
        background-color: rgb(119, 221, 119);
        border-radius: 2.5rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.2), 0 10px 20px rgba(0,0,0,0.2);
    }

    :global(html.dark) nav ul{
        background-color: rgb(65, 138, 65);
    }

    nav ul li{
        display: flex;
        font-size: 1rem;
        align-items: center;
        color: whitesmoke;
        text-shadow: 2px 2px 10px rgb(0, 0, 0);

    }
    nav ul li:hover{
        font-weight: bold;
        cursor: pointer;
        text-shadow: 2px 2px 5px rgb(0, 0, 0)

    }

    nav ul li a{
        color: whitesmoke;
        text-decoration: none;
    }

    /* .sideBar {
        top: 2vh;
        left: 2vh;
        position: fixed;
        width: 50vw;
        display: ;
        z-index: 1000;
        display: none; 
        transform: translateX(-100%); 
        transition: transform 0.9s ease-in-out; 
        z-index: 1000;
    }

    .sideBar.activo{
        display: block;
        transform: translateX(0);

    }

    .sideBar button{
        height: 9vh;
        width: 90%;
        margin: 0 0 2vh 0;
        border-radius: 2rem;
        font-size: 1.5rem;
        border: none;
        cursor: pointer;
        padding: 0.5rem;
    }

    :global(html.dark) .sideBar button{
        background-color: rgb(65, 138, 65);
    }

    .sideBar button svg{
        height: 100%;
    }

    .sideBar ul {
        width: 90%;
        height: 90%;
        border-radius: 2rem;
    }

    :global(html.dark) .sideBar ul{
        background-color: rgb(65, 138, 65);
    }

    .sideBar ul li{
        height: 9vh;
        width: 90%;
        border-radius: 0px;
        font-size: 1.5rem;
    }

    .sideBar .cerrarSideBar{
        position: absolute;
        top: 0;
        left: 70vw;
        width: 9vh;
        height: 9vh;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .overlay{
        display: none; 
        position: fixed;
        width: 100vw;
        height: 100vh;
        top: 0;
        left: 0;
        background-color: rgba(0, 0, 0, 0.541);
        z-index: 500;
    }

    .overlay.activo{
        display: block;
    } */


    .visionMode path{
        height: 5vh;
        width: 5vh;
    }
    .visionMode:hover {
        cursor: pointer;
        text-shadow: 2px 2px 5px rgb(0, 0, 0)

    }
</style>